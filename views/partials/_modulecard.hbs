<div class="{{module.color}} lighten-3">&nbsp;</div>
<!-- Can swap for image cards-->
<div class="card hoverable">
    <span class="badge {{module.color}} lighten-3">4 unopened</span>
    <div class="card-content">
        <span class="card-title">{{module.name}}</span>
        <p>{{module.description}}</p>
    </div>
    <div class="card-action">
        <a class="btn {{module.color}}" href="/modules/{{module.id}}">Enter</a>
    </div>
    <!-- Add progress bar too-->
</div>